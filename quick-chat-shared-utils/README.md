# quick-chat-shared-utils

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build quick-chat-shared-utils` to build the library.

## Running unit tests

Run `nx test quick-chat-shared-utils` to execute the unit tests via [Jest](https://jestjs.io).
