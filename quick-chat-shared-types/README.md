# quick-chat-shared-types

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build quick-chat-shared-types` to build the library.

## Running unit tests

Run `nx test quick-chat-shared-types` to execute the unit tests via [Jest](https://jestjs.io).
